def power(base, exp):
    result = 1
    while exp > 0:
        if exp % 2 == 1:
            result *= base
        base *= base
        exp //= 2
    return result

def powerOfNumbers(n):
    r = reverse_num(n)
    return power(n, r)
